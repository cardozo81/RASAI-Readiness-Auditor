[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "searchgeo-readiness-auditor"
version = "0.1.0"
description = "RASAI — Search & AI readiness auditor. Legacy Python namespace retained for compatibility."
requires-python = ">=3.13,<3.14"
dependencies = [
    "playwright>=1.57,<2",
    "tzdata>=2026.1",
]

[project.scripts]
rasai = "searchgeo.entrypoint:main"
rasai-console = "searchgeo.console_entrypoint:main"
searchgeo = "searchgeo.entrypoint:main"
searchgeo-console = "searchgeo.console_entrypoint:main"

[tool.setuptools.packages.find]
where = ["src"]