name: RASAi public brand normalization

on:
  push:
    paths:
      - '.github/workflows/rasai-brand-style-normalization.yml'
      - '.github/scripts/rasai_brand_normalize.py'
      - '.github/scripts/rasai_brand_cleanup.py'
      - '.github/scripts/rasai_brand_finalize.py'
      - '.github/scripts/rasai_public_surface_cleanup.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  normalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Normalize public brand
        shell: bash
        run: |
          python .github/scripts/rasai_brand_normalize.py || echo "Pre-cleanup scan found predecessor references; continuing."
      - name: Normalize current-state documentation
        shell: bash
        run: |
          python .github/scripts/rasai_brand_cleanup.py || echo "Additional predecessor references found; continuing to final normalization."
      - name: Final documentation normalization and gate
        run: python .github/scripts/rasai_brand_finalize.py
      - name: Normalize public product surface
        run: python .github/scripts/rasai_public_surface_cleanup.py
      - name: Install package and browser
        run: |
          python -m pip install -e . pytest
          python -m playwright install --with-deps chromium
      - name: Compile
        run: python -m compileall -q src/searchgeo
      - name: Brand and report regression tests
        env:
          PYTHONPATH: .
        run: pytest -q tests/test_rasai_sari_branding.py tests/test_searchgeo_readiness_reporting.py tests/test_report_registry_and_method_docs.py tests/test_m21_web_performance.py tests/test_report_navigation.py
      - name: Full regression suite
        env:
          PYTHONPATH: .
        run: pytest -q
      - name: Remove normalization runners and commit
        run: |
          git rm .github/workflows/rasai-brand-style-normalization.yml .github/scripts/rasai_brand_normalize.py .github/scripts/rasai_brand_cleanup.py .github/scripts/rasai_brand_finalize.py .github/scripts/rasai_public_surface_cleanup.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs src tests pyproject.toml iniciar.cmd
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Standardize public brand as RASAi"
          git push origin HEAD:main
